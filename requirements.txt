# Angel — Windows desktop AI assistant
# Python 3.10–3.12 recommended (3.11/3.12 ideal). All packages ship Windows wheels.

# UI (Qt Quick / QML, GPU accelerated)
PySide6>=6.6,<6.10

# LLM (OpenRouter is OpenAI-compatible)
openai>=1.40,<2

# Local speech-to-text (no torch needed — uses CTranslate2)
faster-whisper>=1.0,<2

# Audio capture + playback (bundles PortAudio on Windows)
sounddevice>=0.4.6,<0.6

# Math / audio buffers
numpy>=1.24,<3

# Config / env
python-dotenv>=1.0,<2

# HTTP (Fish Audio TTS)
requests>=2.31,<3

# MessagePack encoding for Fish Audio API
ormsgpack>=1.4,<2

# Screenshots (fast, multi-monitor)
mss>=9.0,<11

# Image encode/downscale for vision
Pillow>=10.0,<12

# System info / process control
psutil>=5.9,<8

# Windows-only input + volume control (skipped on other platforms)
pyautogui>=0.9.54,<1; sys_platform == 'win32'
pycaw>=20240210; sys_platform == 'win32'
comtypes>=1.2; sys_platform == 'win32'

# Tests
pytest>=8.0,<9
